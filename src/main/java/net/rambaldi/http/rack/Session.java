package net.rambaldi.http.rack;

/**
 rack.session:	A hash like interface for storing request session data.
 The store must implement: store(key, value) (aliased as []=); fetch(key, default = nil) (aliased as []); delete(key); clear;
 */
public class Session {
}
