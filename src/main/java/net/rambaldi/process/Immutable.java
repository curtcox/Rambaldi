package net.rambaldi.process;

/**
 * A marker interface for that which is immutable -- stateless, and unchanging.
 * Objects that implement this interface are claiming not to have state.
 */
public interface Immutable {}
